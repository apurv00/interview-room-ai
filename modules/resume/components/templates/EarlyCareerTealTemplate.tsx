import type { TemplateProps } from './index'
import EarlyCareerLayout from '../layouts/EarlyCareerLayout'

export default function EarlyCareerTealTemplate({ data }: TemplateProps) {
  return <EarlyCareerLayout data={data} variantId="early-career-teal" />
}
