/** Matrix dimensions — sourced from rosterMatrix.mjs (keep runner in sync via qa:build:browser). */

export {
  listMatrixCells,
  matrixCellCount,
  listDomainDepthCombos,
  planShards,
  depthApplies,
  ROSTER_DOMAINS,
  ROSTER_DEPTHS,
  SMOKE_CELLS,
  PERSONAS,
} from './rosterMatrix.mjs'
